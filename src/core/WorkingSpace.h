#pragma once

// The linear Rec. 2020 working space (docs/adr/0001).
//
// Rec. 2020 luma coefficients — must match kLuma in shaders/image.frag.
inline constexpr float kLumaR = 0.2627f;
inline constexpr float kLumaG = 0.6780f;
inline constexpr float kLumaB = 0.0593f;
